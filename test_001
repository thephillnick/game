import random

player_name = input("playername: ")

#begin stats
player_hp = 100
enemie_hp = 100

def show_stats ():
    global enemie_hp
    global player_hp
    if enemie_hp >= 0 and player_hp >= 0:
        print (f"player has {player_hp} remaining ")
        print (f"enemie has {enemie_hp} remaining ")

    elif enemie_hp < 0: #end stage??
        print ("vamos, enemie has been slayn")

    elif player_hp < 0: #randomizer end messages
        print (f"{player_name} has been slayn")    

def global_stats ():
    global enemie_hp
    global player_hp
# attacks
def p_attack_normal (dmg):
    global enemie_hp

    enemie_hp = enemie_hp - dmg
    
    print (f"{player_name} has done {dmg} dmg. ")
    show_stats ()

def e_attack_normal (dmg):
    global player_hp

    player_hp = player_hp - dmg
    print (f"the enemie has done {dmg} dmg. ") #later andere enemies hier invullen

    show_stats ()



    #options
def player_turn ():
    chosen_card = int(input("welke kaart wil je spelen?:"))
    if chosen_card == 1:
        p_attack_normal (80)
    elif chosen_card == 2:
        p_attack_normal (10)
    elif chosen_card == 3:
        p_attack_normal (0)




def enemie_turn ():

    # type_card = random.randint(1,n)

    ##e_dmg_normal = random.randint(3,16)
    # if type_card == 1: (indend hierna later)
    ##e_attack_normal (e_dmg_normal)
    e_attack_normal (15)



    



show_stats()

while enemie_hp > 0 and player_hp > 0:
    global_stats ()

    player_turn ()
    enemie_turn ()
